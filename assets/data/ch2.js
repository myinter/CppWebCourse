/* ============================================================
   第 2 章 · 运算符与表达式

   所有 code.expectedOutput 均由 clang++ 实际编译运行得到，
   由 tools/verify_examples.mjs 校验，不是手写的想象值。
   ============================================================ */
(function (global) {
  'use strict';
  global.PYT = global.PYT || {};
  global.PYT.data = global.PYT.data || {};

  global.PYT.data.ch2 = [

    /* ---------- 1. 封面 ---------- */
    {
      type: 'title',
      eyebrow: '第 2 章',
      title: '运算符与表达式',
      lead: '构建程序的基石 · 让数据"动"起来',
      notes: '大家好，欢迎回到 C/C++ 编程教程。在第一章，我们学习了如何存储数据。在这一章，我们将学习如何操作这些数据。本章的主题是"运算符与表达式"，它们是构建程序逻辑、处理数据的核心工具。掌握了它们，你就能让程序真正地"动"起来。'
    },

    /* ---------- 2. 目录（PPT 第 2 页：本章内容概览） ---------- */
    {
      type: 'toc',
      eyebrow: '本章内容概览',
      title: '八个 PART，一条主线',
      lead: '从最熟悉的算术运算出发，一路走到"表达式为什么会算错"—— 每个 PART 里都埋着坑，也都有对应的提示卡。',
      items: [
        { num: '01', title: '算术运算符', body: '最熟悉的计算 —— 加、减、乘、除、取模，除法和取模最容易出错' },
        { num: '02', title: '关系运算符', body: '比较大小 —— 结果是布尔值（真/假），常用于条件判断' },
        { num: '03', title: '逻辑运算符', body: '组合条件 —— 通过与、或、非把多个比较串成一个复杂条件' },
        { num: '04', title: '位运算符', body: '直接操作二进制 —— 一个一个比特地算，离硬件最近' },
        { num: '05', title: '赋值与复合赋值', body: '给变量赋值 —— `x += 5` 是 `x = x + 5` 的简洁写法' },
        { num: '06', title: '自增 / 自减', body: '变量的增减 —— 前置与后置的区别，本章的重点和难点' },
        { num: '07', title: '优先级与结合性', body: '运算的先后顺序 —— 谁先算、从哪边算，拿不准就加括号' },
        { num: '08', title: '求值顺序与副作用', body: '潜在的陷阱 —— 同一个表达式里改两次变量，就是未定义行为' }
      ],
      notes: '本章我们将学习八大类运算符。从最基础的算术运算，到用于判断的关系和逻辑运算，再到底层的位运算。我们还会学习赋值、自增自减等常用操作，并深入理解运算的优先级和潜在的副作用问题。让我们开始吧！'
    },

    /* ---------- 3. 分节 01 ---------- */
    {
      type: 'section',
      num: '01',
      title: '算术运算符',
      sub: '最熟悉的计算',
      lead: '加、减、乘、除、取模 —— 五个符号看着眼熟，其中两个藏着本章第一个大坑。',
      notes: '首先，我们来看最熟悉的算术运算符。它们用于执行基本的数学运算，就像我们在数学课上学的一样，包括加、减、乘、除和取模。'
    },

    /* ---------- 4. 五个算术运算符 ---------- */
    {
      type: 'cards',
      eyebrow: 'PART 01',
      title: '五个算术运算符',
      lead: '规则和数学课一样，只有两点不同：乘号要写成 `*`，除法要看操作数的类型。',
      cards: [
        {
          icon: 'math',
          heading: '+ 加法',
          body: '`5 + 3` 得到 `8`。整数加整数得整数，浮点加浮点得浮点，两边类型不同时会先做隐式转换。'
        },
        {
          icon: 'unbox',
          heading: '- 减法',
          body: '`5 - 3` 得到 `2`。它和加法属于**同一优先级**，从左往右依次计算。'
        },
        {
          icon: 'calculator',
          heading: '* 乘法',
          body: '`5 * 3` 得到 `15`。**编程里没有 `×`**，乘法一律写成星号 `*`，写 `5 × 3` 会直接编译不过。'
        },
        {
          icon: 'chart',
          heading: '/ 除法',
          body: '`5 / 3` 得到 `1` —— 两个整数相除，只保留商的整数部分。**两个操作数里只要有一个是浮点，才做真正的除法。**'
        },
        {
          icon: 'grid',
          heading: '% 取模',
          body: '`5 % 3` 得到 `2`，求的是余数。它和除法是一对：`(a / b) * b + a % b` 一定等于 `a`。**`%` 只能用于整数。**'
        }
      ],
      note: {
        kind: 'trap',
        title: '整数除法：10 / 3 得到 3，不是 3.333',
        text: '两个 `int` 相除，小数部分被**直接丢掉**（不是四舍五入）。想让结果是小数，至少要有一个操作数是浮点：`10.0 / 3` 才是 `3.33333`。这个坑在算平均值、算百分比、算温度转换时最容易踩到 —— 第 1 章那道温度转换练习就是它。'
      },
      notes: '这里是五个基本的算术运算符。加、减、乘都很好理解。需要特别注意的是除法和取模。当操作数都是整数时，除法会执行整数除法，结果只保留商的整数部分。而取模运算则是求除法的余数。'
    },

    /* ---------- 5. 代码示例：算术运算 ---------- */
    {
      type: 'split',
      eyebrow: 'PART 01 · 动手试试',
      title: '同是除法，两种结果',
      lead: '一个 `.0` 之差，结果就完全不同。这段代码请务必亲手跑一遍。',
      points: [
        '`a / b`：两边都是 `int`，走**整数除法**，小数被丢掉',
        '`10.0 / b`：只要一边是浮点，就走浮点除法',
        '`%` 求余数：`10 % 3` 是 `1`，它和除法配合能拆出一个数的每一位',
        '负数的结果要小心：`-7 / 3` 是 `-2`，`-7 % 3` 是 `-1`',
        '**乘法最容易溢出**：两个 `int` 相乘，结果还是 `int`（第 1 章讲过）'
      ],
      code: {
        file: 'arith.cpp',
        source: `#include <iostream>

int main() {
    int a = 10, b = 3;

    // 两边都是整数：执行整数除法，小数部分被直接丢掉
    std::cout << "a / b = " << a / b << std::endl;
    std::cout << "a % b = " << a % b << std::endl;

    // 只要有一个操作数是浮点，就按浮点除法算
    double c = 10.0, d = 3.0;
    std::cout << "c / d = " << c / d << std::endl;
    std::cout << "10.0 / b = " << 10.0 / b << std::endl;

    // 负数取模：结果的符号跟着被除数走
    std::cout << "-7 / 3 = " << -7 / 3 << std::endl;
    std::cout << "-7 % 3 = " << -7 % 3 << std::endl;
    std::cout << "7 % -3 = " << 7 % -3 << std::endl;

    return 0;
}`,
        expectedOutput: `a / b = 3
a % b = 1
c / d = 3.33333
10.0 / b = 3.33333
-7 / 3 = -2
-7 % 3 = -1
7 % -3 = 1`,
        note: {
          kind: 'trap',
          title: '负数取模，和数学上的"余数"不一样',
          text: '数学里我们习惯让余数非负（`-7 = 3 × (-2) + (-1)` 也可以写成 `-7 = 3 × (-3) + 2`）。C++ 的选择是：**商的取整方向是"向零截断"，余数的符号跟着被除数走**。所以 `-7 / 3` 是 `-2`（不是 `-3`），`-7 % 3` 是 `-1`（不是 `2`）。写"每 3 个分一组"这类逻辑时，先确认数据不会是负数。'
        }
      },
      notes: '我们来看一个代码示例。当 a 和 b 都是整数时，a/b 执行整数除法，结果是 3。而当 c 和 d 是浮点数时，c/d 执行浮点数除法，得到了精确的小数结果。这个区别非常重要，是初学者容易出错的地方。'
    },

    /* ---------- 6. 分节 02 ---------- */
    {
      type: 'section',
      num: '02',
      title: '关系运算符',
      sub: 'COMPARISON OF VALUES / 比较大小',
      lead: '关系运算就像天平称重：比较两边的大小，得出"平衡"或"倾斜"的布尔结论。',
      notes: '接下来是关系运算符。它们用于比较两个值的关系，比如判断一个数是否大于另一个数。关系运算的结果是一个布尔值，要么是真，要么是假。'
    },

    /* ---------- 7. 关系运算符速查表 ---------- */
    {
      type: 'compare',
      eyebrow: 'PART 02',
      title: '六个关系运算符',
      lead: '它们的结果永远是 `bool`：真显示为 `true`（本质是 1），假显示为 `false`（本质是 0）。',
      table: {
        head: ['运算符', '名称', '示例', '结果'],
        rows: [
          ['`==`', '等于', '`5 == 3`', '`false` (0)'],
          ['`!=`', '不等于', '`5 != 3`', '`true` (1)'],
          ['`>`', '大于', '`5 > 3`', '`true` (1)'],
          ['`<`', '小于', '`5 < 3`', '`false` (0)'],
          ['`>=`', '大于等于', '`5 >= 5`', '`true` (1)'],
          ['`<=`', '小于等于', '`5 <= 3`', '`false` (0)']
        ]
      },
      note: {
        kind: 'trap',
        title: '把 = 和 == 搞混，是初学者第一大错',
        text: '`=` 是**赋值**，`==` 才是**判断相等**。`if (x = 5)` 不会报错，它的意思是"把 5 赋给 x，然后判断 5 是不是真"—— 条件永远成立，你的判断逻辑彻底失效。防身技巧：判断常量和变量是否相等时，把常量写在左边（`5 == x`），万一漏写一个等号，编译器会直接报错。'
      },
      notes: '关系运算符包括等于、不等于、大于、小于等等。它们的结果是布尔值，在 C++ 中用 1 代表真，0 代表假。这里要特别强调，判断两个值是否相等要用两个等号 ==，而一个等号 = 是用来赋值的，千万不要搞混。这是很多初学者在编写条件判断时最容易犯的错误。'
    },

    /* ---------- 8. 代码示例：关系运算 ---------- */
    {
      type: 'split',
      eyebrow: 'PART 02 · 动手试试',
      title: '把比较的结果存进 bool 变量',
      lead: '关系运算的结果可以被存起来、被打印，也可以直接拿去参与运算。',
      points: [
        '`bool is_pass = (score >= 60);` —— 比较的结果是一个 `bool`',
        '`std::boolalpha` 让输出变成 `true` / `false`，而不是 `1` / `0`',
        '`bool` 参与算术运算时，`true` 当 `1`、`false` 当 `0` 用',
        '关系运算真正的舞台是 `if`、`while` 这些流程控制语句（第 3 章）'
      ],
      code: {
        file: 'relation.cpp',
        source: `#include <iostream>

int main() {
    int score = 85;
    bool is_pass = (score >= 60);

    // boolalpha 让 bool 输出 true/false 而不是 1/0
    std::cout << std::boolalpha;
    std::cout << "成绩是否及格？" << is_pass << std::endl;

    int num1 = 10, num2 = 20;
    std::cout << "num1 < num2: " << (num1 < num2) << std::endl;
    std::cout << "num1 == num2: " << (num1 == num2) << std::endl;

    // 关系运算的结果是 bool，本质上就是 1 和 0
    std::cout << "(num1 < num2) + (num1 == num2) = "
              << (num1 < num2) + (num1 == num2) << std::endl;

    return 0;
}`,
        expectedOutput: `成绩是否及格？true
num1 < num2: true
num1 == num2: false
(num1 < num2) + (num1 == num2) = 1`,
        note: {
          kind: 'note',
          title: 'boolalpha 是"设置一次、一直生效"的流标志',
          text: '`std::boolalpha` 一旦写进 `std::cout`，**后面所有的 `bool` 都会打印成 `true` / `false`**，直到用 `std::noboolalpha` 关掉。所以最后一行的 `1` 并不是它"没生效"：`(num1 < num2) + (num1 == num2)` 是 `bool` 相加，结果类型是 `int`，本来就不受 `boolalpha` 影响。'
        }
      },
      notes: '在代码中，我们可以将关系运算的结果存储在布尔类型的变量中。通过 boolalpha 这个控制符，我们可以让程序输出 true 或 false，而不是默认的 1 或 0，这样结果更直观。关系运算通常用在后面会学到的 if 条件判断中。'
    },

    /* ---------- 9. 分节 03 ---------- */
    {
      type: 'section',
      num: '03',
      title: '逻辑运算符',
      sub: 'COMBINING CONDITIONS / 组合条件',
      lead: '"年龄大于 18 并且有票才能进" —— 当判断条件不止一条时，用逻辑运算符把它们组合起来。',
      notes: '有时候，我们需要判断更复杂的条件，比如"年龄大于18并且有票"才能进入。这时就需要用到逻辑运算符来组合多个关系表达式。'
    },

    /* ---------- 10. 与或非 ---------- */
    {
      type: 'cards',
      eyebrow: 'PART 03',
      title: '与 · 或 · 非',
      lead: '三个运算符，三句口诀。它们作用的对象是"真/假"，不是数字。',
      cards: [
        {
          icon: 'logic',
          heading: '`&&` 逻辑与 AND',
          body: '两边**都**为真，结果才为真。口诀：**同真为真，一假则假**。例：`(age > 18) && has_ticket`'
        },
        {
          icon: 'branch',
          heading: '`||` 逻辑或 OR',
          body: '只要**有一边**为真，结果就为真。口诀：**一真为真，同假则假**。例：`is_student || is_weekend`'
        },
        {
          icon: 'convert',
          heading: '`!` 逻辑非 NOT',
          body: '取反：真变假，假变真。口诀：**颠倒黑白**。例：`!can_enter` 在 `can_enter` 为真时得到 `false`'
        }
      ],
      note: {
        kind: 'trap',
        title: '`!` 的优先级比比较运算符高',
        text: '`!a == b` 不是 `!(a == b)`，而是 `(!a) == b` —— 因为 `!` 是一元运算符，优先级排在 `==` 前面。想表达"a 和 b 不相等"，请写 `!(a == b)` 或干脆写 `a != b`。这类"少写一个括号"的错误，编译器不会提醒你。'
      },
      notes: '逻辑运算符有三个：与、或、非。逻辑与 && 要求两边的表达式都为真，结果才为真。逻辑或 || 只要有一边为真，结果就为真。逻辑非 ! 则是取反操作。大家可以记住这个口诀，帮助理解。'
    },

    /* ---------- 11. 代码示例：逻辑运算 ---------- */
    {
      type: 'split',
      eyebrow: 'PART 03 · 动手试试',
      title: '组合条件：能不能进、能不能打折',
      lead: '把两个关系表达式用 `&&` 或 `||` 连起来，就得到了一个更复杂的条件。',
      points: [
        '`&&` 要求两个条件**同时成立**：满 18 岁**并且**有票',
        '`||` 只要**有一个成立**即可：是学生**或者**是周末',
        '逻辑运算符两边通常放的是**关系表达式**（比较的结果）',
        '括号不是语法要求，但 `(age > 18) && has_ticket` 比 `age > 18 && has_ticket` 好读得多'
      ],
      code: {
        file: 'logic.cpp',
        source: `#include <iostream>

int main() {
    int age = 20;
    bool has_ticket = true;

    // 逻辑与 &&：两个条件都成立，结果才为真
    bool can_enter = (age > 18) && has_ticket;
    std::cout << std::boolalpha;
    std::cout << "能否进入？" << can_enter << std::endl;

    bool is_student = false;
    bool is_weekend = true;

    // 逻辑或 ||：只要有一个条件成立，结果就为真
    bool can_discount = is_student || is_weekend;
    std::cout << "能否享受折扣？" << can_discount << std::endl;

    // 逻辑非 !：真变假，假变真
    std::cout << "!can_enter = " << !can_enter << std::endl;

    return 0;
}`,
        expectedOutput: `能否进入？true
能否享受折扣？true
!can_enter = false`
      },
      notes: '这个例子展示了逻辑与和逻辑或的用法。can_enter 要求年龄大于 18 和有票两个条件同时满足。而 can_discount 只要求是学生或者是周末其中一个条件满足即可。通过逻辑运算符，我们可以构建非常复杂的判断逻辑。'
    },

    /* ---------- 12. 短路求值 ---------- */
    {
      type: 'split',
      eyebrow: 'PART 03 · 高级特性',
      title: '短路求值：能不算就不算',
      lead: '`&&` 的左边一旦为假，右边**根本不会执行**；`||` 的左边一旦为真，右边同样不会执行。',
      points: [
        '`a == 0 || ++a > 5`：左边已经为真，右边的 `++a` **没有执行**，所以 `a` 还是 `0`',
        '`b != 0 && ++b > 0`：左边已经为假，右边的 `++b` 同样被跳过',
        '对比实验：把 `&&` 换成不短路的 `&`，右边的 `++c` 就真的执行了',
        '**省算力**：省掉那些不必要的计算；**保安全**：挡住后面可能出事的操作'
      ],
      code: {
        file: 'shortcircuit.cpp',
        source: `#include <iostream>

int main() {
    // || 短路：左边已经为真，右边根本不会执行
    int a = 0;
    if (a == 0 || ++a > 5) {
        std::cout << "条件成立" << std::endl;
    }
    std::cout << "a = " << a << std::endl;

    // && 短路：左边已经为假，右边同样不会执行
    int b = 0;
    if (b != 0 && ++b > 0) {
        std::cout << "不会走到这里" << std::endl;
    }
    std::cout << "b = " << b << std::endl;

    // 对比：不短路的话，++b 会照常执行
    int c = 0;
    bool flag = (c != 0) & (++c > 0);   // 注意这里是一个 &，没有短路
    std::cout << "flag = " << flag << ", c = " << c << std::endl;

    return 0;
}`,
        expectedOutput: `条件成立
a = 0
b = 0
flag = 0, c = 1`,
        note: {
          kind: 'trap',
          title: '短路是双刃剑：副作用会被"吃掉"',
          text: '右边的表达式**可能永远不执行**，所以千万别把必须执行的操作（自增、赋值、函数调用）放在右边：`if (i < n && arr[++i] > 0)` 这种写法迟早出错。反过来，短路也是防御性编程的利器 —— 以后你会经常写 `if (p != nullptr && *p > 0)`：指针一旦为空，右边的解引用就被短路挡住了；要是把顺序写反成 `*p > 0 && p != nullptr`，程序会直接崩溃。'
        }
      },
      notes: '逻辑运算符还有一个非常巧妙的特性叫短路求值。比如在使用 || 时，如果第一个表达式已经是真，计算机就知道整个表达式的结果肯定是真，所以它会"偷懒"，不再计算第二个表达式。这个特性不仅能提高效率，有时还能帮助我们避免错误。'
    },

    /* ---------- 13. 分节 04 ---------- */
    {
      type: 'section',
      num: '04',
      title: '位运算符',
      sub: '直接操作二进制',
      lead: '这一节最抽象，但它离硬件最近 —— 把整数拆成一个个比特来看，规则其实和逻辑运算一模一样。',
      notes: '接下来我们要学习一类更底层的运算符——位运算符。它们直接对整数在内存中的二进制位进行操作，这体现了 C/C++ 语言贴近硬件的特性，非常高效。'
    },

    /* ---------- 14. 六个位运算符 + 二进制演算 ---------- */
    {
      type: 'cards',
      eyebrow: 'PART 04',
      title: '六个位运算符：一位一位地算',
      lead: '规则和逻辑运算完全相同，区别只在于：逻辑运算看的是"整个值"，位运算看的是**每一个二进制位**。下面用 `5`（`101`）和 `3`（`011`）演算一遍。',
      cards: [
        {
          icon: 'layers',
          heading: '`&` 按位与',
          body: '对应位**都是 1** 才得 1，否则为 0。`5 & 3` → `101 & 011 = 001` → **1**'
        },
        {
          icon: 'grid',
          heading: '`|` 按位或',
          body: '对应位**有一个是 1** 就得 1。`5 | 3` → `101 | 011 = 111` → **7**'
        },
        {
          icon: 'compare',
          heading: '`^` 按位异或',
          body: '对应位**不相同**得 1，相同得 0。`5 ^ 3` → `101 ^ 011 = 110` → **6**'
        },
        {
          icon: 'convert',
          heading: '`~` 按位取反',
          body: '每个 0 变 1、1 变 0。`~5` 按 8 位看是 `11111010`，即无符号的 **250**；当成有符号 `int` 则是 **-6**'
        },
        {
          icon: 'move',
          heading: '`<<` 左移',
          body: '各位左移，右边补 0，效果**等于乘 2**。`1 << 3` → `1000` 是 **8**；`5 << 1` → `1010` 是 **10**'
        },
        {
          icon: 'steps',
          heading: '`>>` 右移',
          body: '各位右移，左侧补符号位，效果**等于除 2**（丢余数）。`5 >> 1` → `10` 是 **2**'
        }
      ],
      note: {
        kind: 'trap',
        title: '位运算只认整数，负数更要小心',
        text: '三点提醒：① `double`、`float` **不能**做位运算，编译器直接报错；② 负数在内存里是**补码**，同一个位模式可以有完全不同的解释 —— `~5` 从无符号角度看是 `250`，当成 `int` 又是 `-6`；③ 移位的位数不要超过类型宽度，`1 << 31` 会把符号位挤掉，结果不再可靠，写 `>>` 时也要留意负数左补的是符号位还是 0。位运算效率极高，常用于底层驱动、图形处理等场景。'
      },
      notes: '位运算符包括按位与、或、异或、取反，以及左移和右移。它们的规则和逻辑运算类似，但作用在每一个二进制位上。例如，左移一位通常相当于乘以2，右移一位相当于除以2，而且这种运算比乘法除法要快得多。'
    },

    /* ---------- 15. 代码示例：位运算 ---------- */
    {
      type: 'split',
      eyebrow: 'PART 04 · 动手试试',
      title: '在代码里做位运算',
      lead: '用 `unsigned char` 演示最直观：它正好 8 位，一眼能数清。',
      points: [
        '`unsigned char a = 6;` —— 二进制是 `0000 0110`',
        '`a & b`、`a | b`、`a ^ b` 逐位算出来是 `2`、`7`、`5`',
        '输出时必须转成 `(int)`，否则 `cout` 会把它当**字符**打印',
        '`<<` 移一位等于乘 2，`>>` 移一位等于除 2，比乘除法指令更快',
        '位运算的实际用途：底层驱动、图形处理、数据压缩、加密算法'
      ],
      code: {
        file: 'bitwise.cpp',
        source: `#include <iostream>

int main() {
    unsigned char a = 6;   // 二进制 0000 0110
    unsigned char b = 3;   // 二进制 0000 0011

    // 转成 int 再输出，否则 cout 会把它当成字符打印
    std::cout << "a & b  = " << (int)(a & b) << std::endl;
    std::cout << "a | b  = " << (int)(a | b) << std::endl;
    std::cout << "a ^ b  = " << (int)(a ^ b) << std::endl;
    std::cout << "~a     = " << (int)(unsigned char)(~a) << std::endl;
    std::cout << "a << 2 = " << (int)(a << 2) << std::endl;
    std::cout << "a >> 1 = " << (int)(a >> 1) << std::endl;

    // 左移一位等于乘 2，右移一位等于除 2（这里用的是 int）
    int n = 5;
    std::cout << "5 << 1 = " << (n << 1) << std::endl;
    std::cout << "5 >> 1 = " << (n >> 1) << std::endl;

    return 0;
}`,
        expectedOutput: `a & b  = 2
a | b  = 7
a ^ b  = 5
~a     = 249
a << 2 = 24
a >> 1 = 3
5 << 1 = 10
5 >> 1 = 2`,
        note: {
          kind: 'note',
          title: '为什么一定要写 (int)',
          text: '`cout << a` 里的 `a` 是 `unsigned char`，`cout` 认为它是一个**字符**，会照编码表打印出看不见的控制符号，而不是数字 `6`。另外，`char` 这种比 `int` 窄的类型在参与算术运算前会先发生**整型提升**（先变成 `int` 再算），所以 `(int)(a & b)` 这种"先算完整型提升、再转换"的写法才是稳妥的。'
        }
      },
      notes: '我们来看一个具体的例子。变量 a 和 b 的二进制形式分别是 00000110 和 00000011。通过按位与、或、异或操作，我们得到了不同的结果。左移和右移操作也直观地展示了乘以2和除以2的效果。位运算在很多底层开发领域非常有用。'
    },

    /* ---------- 16. 分节 05 ---------- */
    {
      type: 'section',
      num: '05',
      title: '赋值运算符',
      sub: '给变量赋值 / ASSIGNMENT OPERATORS',
      lead: '`x = x + 5` 写三遍就烦了 —— C++ 给你的回报是 `x += 5`。',
      notes: '赋值运算符是我们最常用的运算符之一，它用于给变量赋值。除了基本的赋值，C++还提供了复合赋值，可以让代码更简洁。'
    },

    /* ---------- 17. 赋值与复合赋值 ---------- */
    {
      type: 'split',
      eyebrow: 'PART 05',
      title: '赋值与复合赋值',
      lead: '复合赋值不是新运算，它只是"读一次、算一次、写回去"的简写。',
      points: [
        '基本赋值 `x = 10;` —— 把右边的值放进左边的变量',
        '复合赋值 `x += 5;` **完全等价于** `x = x + 5;`',
        '五个算术运算符都有对应的复合写法：`+=` `-=` `*=` `/=` `%=`',
        '赋值是**右结合**的：`a = b = c = 7;` 从右往左算（PART 07 细讲）',
        '好处：写得短、读得清楚，也更容易被编译器优化'
      ],
      code: {
        file: 'assign.cpp',
        source: `#include <iostream>

int main() {
    int x = 10;
    std::cout << "初始 x = " << x << std::endl;

    x += 5;
    std::cout << "x += 5  -> " << x << std::endl;
    x -= 3;
    std::cout << "x -= 3  -> " << x << std::endl;
    x *= 2;
    std::cout << "x *= 2  -> " << x << std::endl;
    x /= 5;
    std::cout << "x /= 5  -> " << x << std::endl;
    x %= 3;
    std::cout << "x %= 3  -> " << x << std::endl;

    // 右边整个先算完，再和左边运算
    int total = 10;
    total *= 2 + 3;   // 等价于 total = total * (2 + 3)
    std::cout << "total = " << total << std::endl;

    // 复合赋值照样遵循整数除法
    int n = 7;
    n /= 2;
    std::cout << "7 /= 2  -> " << n << std::endl;

    // 链式赋值：从右往左算
    int a, b, c;
    a = b = c = 7;
    std::cout << "a = " << a << ", b = " << b << ", c = " << c << std::endl;

    return 0;
}`,
        expectedOutput: `初始 x = 10
x += 5  -> 15
x -= 3  -> 12
x *= 2  -> 24
x /= 5  -> 4
x %= 3  -> 1
total = 50
7 /= 2  -> 3
a = 7, b = 7, c = 7`,
        note: {
          kind: 'trap',
          title: '`x *= 2 + 3` 不是 `x = x * 2 + 3`',
          text: '复合赋值是**整体打包**的：`x *= 2 + 3` 展开后是 `x = x * (2 + 3)`，也就是乘 5（上面 `total` 从 `10` 变成 `50` 就是这个道理），而不是先乘 2 再加 3。另外别忘了，复合赋值里的除法照样是**整数除法**：`7 /= 2` 得到 `3`，不是 `3.5`。'
        }
      },
      notes: '除了 x = x + 5 这种写法，我们可以用更简洁的 x += 5 来代替。同样，减、乘、除等运算都有对应的复合赋值运算符。使用复合赋值可以让代码更紧凑，也更易于阅读。在实际开发中，这是一种非常推荐的编码习惯。'
    },

    /* ---------- 18. 分节 06 ---------- */
    {
      type: 'section',
      num: '06',
      title: '自增 / 自减运算符',
      sub: '++ · -- · 变量的增减',
      lead: '本章最容易考、也最容易写错的一对符号：`i++` 和 `++i`。',
      notes: '自增 ++ 和自减 -- 是两个非常常用的运算符，用于将变量的值加1或减1。它们有两种形式，前置和后置，这是本章的重点和难点。'
    },

    /* ---------- 19. 前置 vs 后置 ---------- */
    {
      type: 'split',
      eyebrow: 'PART 06',
      title: '前置 vs 后置：先加还是先用',
      lead: '单独成一条语句时它们完全一样；一旦参与运算，结果就分道扬镳。',
      points: [
        '前置 `++a`：**先加后用** —— `a` 先变成 `6`，再把 `6` 交出去',
        '后置 `b++`：**先用后加** —— 先把 `5` 交出去，`b` 再变成 `6`',
        '单独写 `i++;` 或 `++j;` 时，两者**没有区别**（编译器会生成同样的指令）',
        '差别只在"结果被用到"的时候显现：`int c = ++a;` 和 `int d = b++;`',
        '自减 `--` 的规则完全一样，只是把加 1 换成减 1'
      ],
      code: {
        file: 'incdec.cpp',
        source: `#include <iostream>

int main() {
    // 写在表达式里：前置和后置的结果不同
    int a = 5, b = 5;
    int c = ++a;   // a 先变成 6，再把 6 赋给 c
    int d = b++;   // 先把 5 赋给 d，b 再变成 6
    std::cout << "a = " << a << ", c = " << c << std::endl;
    std::cout << "b = " << b << ", d = " << d << std::endl;

    // 单独成一条语句：两者完全等价
    int i = 0, j = 0;
    i++;
    ++j;
    std::cout << "i = " << i << ", j = " << j << std::endl;

    // 直接用 cout 打印，差别看得最清楚
    int m = 10, n = 10;
    std::cout << "m++ = " << m++ << std::endl;
    std::cout << "++n = " << ++n << std::endl;
    std::cout << "现在 m = " << m << ", n = " << n << std::endl;

    // 自减同理
    int k = 5;
    std::cout << "k-- = " << k-- << std::endl;
    std::cout << "--k = " << --k << std::endl;

    return 0;
}`,
        expectedOutput: `a = 6, c = 6
b = 6, d = 5
i = 1, j = 1
m++ = 10
++n = 11
现在 m = 11, n = 11
k-- = 5
--k = 3`,
        note: {
          kind: 'trap',
          title: '别把自增塞进复杂的表达式里',
          text: '`cout << m++ << ++n;` 这种写法虽然合法，但**一眼看不出到底打印了哪个值**。记住两条：① 自增自减**尽量单独成一条语句**；② 如果结果要被用到，写清楚是"新值"还是"旧值"——需要旧值就先存一个变量。PART 08 会看到，把多个自增塞进同一个表达式，甚至会变成未定义行为。'
        }
      },
      notes: '前置自增 ++x，是先把 x 的值加1，再使用这个新的值。而后置自增 x++，是先使用 x 原来的值，然后再把 x 的值加1。这个细微的差别在表达式中会产生完全不同的结果，大家一定要分清楚。'
    },

    /* ---------- 20. 与 CPU 指令的关系 ---------- */
    {
      type: 'cards',
      eyebrow: 'PART 06 · 深入理解',
      title: '为什么 ++ 能这么快',
      lead: '因为 `++` 和 `--` 可以直接对上 CPU 的**一条**汇编指令 —— 这不是语法糖，是硬件本来就有的动作。',
      cards: [
        {
          icon: 'compile',
          heading: '前置 `++i` → 一条 `INC`',
          body: '编译器通常把它直接翻译成一条 `INC`（Increment）指令：对寄存器里的值加一，没有任何中间步骤，是效率最高的写法。'
        },
        {
          icon: 'compare',
          heading: '后置 `i++` 要多做一步',
          body: '逻辑上必须**先保存原来的值**再自增。这会引入临时寄存器的读写，对应多条汇编指令 —— 在循环这种高频场景里，开销会被放大。'
        },
        {
          icon: 'sparkle',
          heading: '零开销抽象',
          body: 'C/C++ 的设计哲学：语法糖不带来运行时开销。你写的是易读的 `++i`，跑起来接近手写汇编的效率。'
        }
      ],
      note: {
        kind: 'note',
        title: '那平时到底写哪个？',
        text: '对 `int` 这种内置类型，当两者结果一样时，现代编译器会把 `i++` 也优化掉，**性能上通常看不出差别**。但 `++i` 的语义更直接（"加一，然后用新值"），读完不用在脑子里绕一圈，所以是更被推荐的写法。等到以后学到自定义类型时你会看到，`i++` 真的要多拷贝一份对象，那时这个习惯就更是必要的了。'
      },
      notes: '为什么自增自减运算符这么常用呢？因为它们非常高效。前置自增 ++i 通常会被编译器直接翻译成CPU的一条自增指令 INC，这是一个非常快的操作。这体现了C++语言追求高效的设计哲学。'
    },

    /* ---------- 21. 分节 07 ---------- */
    {
      type: 'section',
      num: '07',
      title: '优先级与结合性',
      sub: '运算的先后顺序',
      lead: '"谁先算"和"同级从哪边开始算"，是两个不同的问题。',
      notes: '当一个表达式里有多个运算符时，哪个先算呢？这就由运算符的优先级和结合性来决定。理解它们，可以帮助我们写出正确的表达式。'
    },

    /* ---------- 22. 优先级速查表 ---------- */
    {
      type: 'compare',
      eyebrow: 'PART 07',
      title: '常用运算符优先级速查',
      lead: '**优先级**决定不同运算符谁先算（比如 `*` 高于 `+`）；**结合性**决定同级运算符从哪边开始算（多数从左往右，赋值从右往左）。',
      table: {
        head: ['优先级', '运算符', '结合方向'],
        rows: [
          ['1（最高）', '`()` `[]`、后缀 `++` `--`', '左 → 右'],
          ['2', '前缀 `++` `--`、`!` `~`、一元 `-`、`(int)`', '右 → 左'],
          ['3', '`*` `/` `%`', '左 → 右'],
          ['4', '`+` `-`', '左 → 右'],
          ['5', '`<<` `>>`', '左 → 右'],
          ['6', '`<` `<=` `>` `>=`', '左 → 右'],
          ['7', '`==` `!=`', '左 → 右'],
          ['8', '`&` `^` `|`（三者内部 `&` > `^` > `|`）', '左 → 右'],
          ['9', '`&&`', '左 → 右'],
          ['10', '`||`', '左 → 右'],
          ['11（最低）', '`=` `+=` `-=` `*=` `/=` `%=`', '右 → 左']
        ]
      },
      note: {
        kind: 'trap',
        title: '经典误读：`if (x & 1 == 0)`',
        text: '`==` 的优先级**高于** `&`，所以这个条件实际是 `x & (1 == 0)`，也就是 `x & 0`，结果**永远是假**。想判断"最低位是不是 0"，必须老老实实写 `(x & 1) == 0`。而**括号的优先级是最高的**，所以真正的实践建议只有一条：**拿不准就加括号** —— `(a + b) / 2` 和 `a + b / 2` 是两个完全不同的东西，前者的意图一眼就能看懂。'
      },
      notes: '简单来说，优先级就是先算谁的问题，比如先乘除后加减。结合性是同级运算从哪边开始算的问题，比如减法是从左到右。但我给大家的建议是，不用去死记硬背这个复杂的规则，当你不确定的时候，直接用括号 () 把想要先算的部分括起来，这样既清晰又不容易出错。'
    },

    /* ---------- 23. 分节 08 ---------- */
    {
      type: 'section',
      num: '08',
      title: '求值顺序与副作用',
      sub: '潜在的陷阱',
      lead: '本章的最后一节，也是最重要的一节：一个表达式里，同一个变量只能改一次。',
      notes: '最后，我们来讨论一个高级但非常重要的话题：表达式的求值顺序与副作用。这是编写健壮代码时需要特别注意的陷阱。'
    },

    /* ---------- 24. 副作用 ---------- */
    {
      type: 'split',
      eyebrow: 'PART 08',
      title: '副作用：表达式也会"改东西"',
      lead: '"副作用"不是贬义词，它指的是一个表达式除了算出结果，**还顺手修改了别的状态**。',
      points: [
        '`i++` 既交出了 `i` 的旧值，**又把 `i` 加了 1** —— 这就是副作用',
        '`--`、`=`、`+=` 这些同样带副作用，都会改变变量的值',
        '纯计算没有副作用：`x * y + x - y` 里 `x`、`y` 只读不写，怎么算都不会错',
        '**核心原则**：同一个表达式里，一个变量只改**一次**'
      ],
      code: {
        file: 'sideeffect.cpp',
        source: `#include <iostream>

int main() {
    // i++ 既返回了值，又修改了 i —— 这就是「副作用」
    int i = 1;
    int a = i++;   // 一条语句里只改一次 i
    int b = i++;   // 再改一次 i，写在另一条语句里
    int sum = a + b;
    std::cout << "a = " << a << ", b = " << b
              << ", sum = " << sum << ", i = " << i << std::endl;

    // 同一个变量在一行里只读不写，随便写都不会出问题
    int x = 3, y = 4;
    int z = x * y + x - y;
    std::cout << "z = " << z << std::endl;

    // 复合赋值是一条语句里最安全的"又读又写"
    std::cout << "x = x + 10 -> " << (x += 10) << std::endl;

    return 0;
}`,
        expectedOutput: `a = 1, b = 2, sum = 3, i = 3
z = 11
x = x + 10 -> 13`,
        note: {
          kind: 'trap',
          title: '❌ 未定义行为：`int j = i++ + i++;`',
          text: '这一行在同一个表达式里对 `i` **读了两次、又改了两次**。C++ 标准没有规定先算左边还是先算右边，所以 `j` 到底是多少**没有任何保证** —— 换个编译器、甚至只是换个优化等级，结果都可能不一样。这种程序的行为叫**未定义行为（Undefined Behavior，UB）**：它不一定会报错，可能今天"看起来正常"，明天改了编译选项就出问题。**正确写法是拆成多条语句**，让每一行只改一次变量 —— 就像上面代码里 `a` 和 `b` 的写法。'
        }
      },
      notes: '副作用指的是在计算表达式时修改了变量的值。如果在同一个表达式中多次修改同一个变量，比如 i++ + i++，由于C++标准没有规定子表达式的计算顺序，不同的编译器可能会得到不同的结果，这被称为未定义行为。为了写出可靠的代码，我们应该避免这样的写法。'
    },

    /* ---------- 25. 反面教材：未定义行为 ---------- */
    {
      type: 'code',
      eyebrow: 'PART 08 · 反面教材',
      title: '这段代码，请永远不要写',
      lead: '它能编译、能运行 —— 这才是它最危险的地方。',
      points: [
        '`int j = i++ + i++;` 违反了"一个表达式里只改一次变量"的原则',
        '它**不报错**：编译器不会提醒你，程序照样跑完',
        '程序本身没有依赖 `i`、`j` 的值，所以这里的输出是固定的；但 `i`、`j` 的值**不可信**',
        '同样危险的一类写法：`f(i++, i++)`、`a[i] = i++;`、`i = i++;`',
        '写每一行之前先问自己：**这一行里有几个地方在改同一个变量？**'
      ],
      code: {
        file: 'undefined_behavior.cpp',
        source: `#include <iostream>

int main() {
    // ❌ 未定义行为：同一个表达式里对 i 读了两次、又改了两次
    int i = 1;
    int j = i++ + i++;

    // 这里故意不打印 i 和 j：它们的值 C++ 标准不做任何保证，
    // 打印出来就等于依赖一个没有保证的结果。
    std::cout << "这行代码能编译、能运行，但结果没有保证。" << std::endl;
    std::cout << "换个编译器、换个优化等级，答案就可能不一样。" << std::endl;

    return 0;
}`,
        expectedOutput: `这行代码能编译、能运行，但结果没有保证。
换个编译器、换个优化等级，答案就可能不一样。`,
        note: {
          kind: 'env',
          title: '关于这段代码',
          text: '示例本身**不含任何输出 `i`、`j` 的语句**，所以它的输出是确定的文本；程序能通过编译、能正常结束。真正不可信的是 `i++ + i++` 这一行的计算结果 —— 本机 clang++ 恰好算出一个值，但那只是巧合，不是保证。演示 UB 时，**绝不要让程序的行为依赖 UB 的结果**，否则你连"它错在哪"都说不清。'
        }
      },
      notes: '这一页是反面教材。这段程序能编译、能运行，甚至能在本机跑出结果，所以很多同学会以为它没问题。请注意：我们故意没有打印 i 和 j，因为它们的结果标准里没有保证。看到 i++ + i++ 这种写法，正确的反应不是"算一下等于几"，而是"这一行必须重写"。'
    },

    /* ---------- 26. 本章小结 ---------- */
    {
      type: 'end',
      icon: 'cpp',
      title: '本章小结',
      lead: '运算符是程序里的"动词"，数据是"名词"。下一章，我们就用这些运算符让程序学会做选择。',
      cards: [
        { icon: 'calculator', heading: '算术 / 关系 / 逻辑', body: '整数除法会丢小数；关系运算返回 `bool`；`&&` `||` 会短路求值' },
        { icon: 'binary', heading: '位运算', body: '逐位操作，`<<` 相当于乘 2、`>>` 相当于除 2；只对整数有效' },
        { icon: 'variable', heading: '赋值与自增', body: '`x += 5` 是 `x = x + 5`；`++i` 先加后用，`i++` 先用后加' },
        { icon: 'alert', heading: '优先级与副作用', body: '拿不准就加括号；同一个表达式里，一个变量只改一次' }
      ],
      notes: '好的，第2章的内容就到这里。我们学习了各种运算符的用法，以及运算的优先级和潜在的陷阱。这些知识是编写程序逻辑的基础。在下一章中，我们将学习如何使用这些运算符来实现程序的流程控制，也就是让程序能够根据不同条件做出不同的选择。感谢大家的观看，我们下节课再见！'
    }
  ];
})(window);
